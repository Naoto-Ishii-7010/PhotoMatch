/**
 * ログインページ（/login）
 * - デザインはFigmaを確認後に実装予定
 */
export default function LoginPage() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-zinc-400">ログインページ（実装予定）</p>
    </div>
  );
}
