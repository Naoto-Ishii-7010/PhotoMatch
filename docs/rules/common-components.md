# Common Components

## 汎用コンポーネントの Props 設計

- 汎用コンポーネントでは、呼び出し元から自由に色や余白などのスタイルを都度指定させすぎない
- 見た目の差分は `variant` や `size` などの Props で切り替えられるように設計する
- 必要に応じて `tone` や `intent` など、意味ベースの Props を追加して表現を統一する
- 呼び出し元で複雑な `className` 調整を前提にせず、コンポーネント側で利用パターンを吸収する
